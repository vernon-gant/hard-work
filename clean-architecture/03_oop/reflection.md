On the advantages side I clearly see that I used the approach where we create contexts objects and pass them from top to bottom and change thus global shared state. I did not use global variables and just passed data around using structs. I also did not use the 100% procedural way and created a mapping between strings and procedures we call for each such string aka command name. From the disadvantages side is that I called the `print` directly from commands without any interface layer.